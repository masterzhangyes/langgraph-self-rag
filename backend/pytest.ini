[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# pytest-asyncio: 自动识别 async 测试（文件内已有的显式 marker 也保持兼容）
asyncio_mode = auto
addopts = -q --disable-warnings --no-header
